## ramp

Extension for editing and sampling of scalar and color (vector3f) ramps, implemented as C++ and Python APIs.
Provided are ready to use ramp UI widgets, that expose the functionality on top level.
