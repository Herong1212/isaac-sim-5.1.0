#usda 1.0
(
    """
    Invalid environment: OpenDrive XODR file doesn't exist.
    """
    defaultPrim = "RootNode"
)

def Xform "RootNode"
{
    custom uniform asset omni:simready:openDrive = @./non_existent.xodr@
}