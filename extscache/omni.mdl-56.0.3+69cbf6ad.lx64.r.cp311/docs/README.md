# MDL Python Bindings [omni.mdl]

This extension provides Python bindings for the MDL SDK.
It consists of two modules:

- pymdlsdk, which is a low-level binding automatically generated from the C++ API using SWIG.
- pymdl, a handwritten high-level binding based on pymdlsdk
