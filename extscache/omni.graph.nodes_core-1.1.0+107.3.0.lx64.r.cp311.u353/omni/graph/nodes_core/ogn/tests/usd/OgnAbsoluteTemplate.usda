#usda 1.0
(
    doc ="""Generated from node description file OgnAbsolute.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_nodes_Absolute" (
        docs="""Compute the absolute value of a scalar, array of scalars, vector, or array of vectors; in the latter three cases the operator is applied to every single scalar element in the corresponding structures."""
    )
    {
        token node:type = "omni.graph.nodes.Absolute"
        int node:typeVersion = 1

        # 1 attribute
        custom token inputs:input (
            docs="""The scalar(s) or vector(s) to take the absolute value of."""
        )

        # 1 attribute
        custom token outputs:absolute (
            docs="""The original "Input Value" with the absolute value operator applied to each scalar
            value. The structure of the result, arrays and tuples, will mirror that of the input."""
        )
    }
}
