[package]
# Semantic Versioning is used: https://semver.org/
version = "2.0.4"

category = "Internal"

# Lists people or organizations that are considered the "authors" of the package.
authors = ["NVIDIA"]

# The title and description fields are primarly for displaying extension info in UI
title = "Geometry Property Widget"
description="View and Edit Geometry Property Values"

# URL of the extension source repository.
repository = ""
support_level = "Core"

# Preview image. Folder named "data" automatically goes in git lfs (see .gitattributes file).
preview_image = "data/preview.png"

# Icon is shown in Extensions window, it is recommended to be square, of size 256x256.
icon = "data/icon.png"

# Keywords for the extension
keywords = ["kit", "usd", "property", "geometry"]

# Location of change log file in target (final) folder of extension, relative to the root.
# More info on writing changelog: https://keepachangelog.com/en/1.0.0/
changelog="docs/CHANGELOG.md"

# Path (relative to the root) or content of readme markdown file for UI.
readme  = "docs/README.md"

[dependencies]
"omni.usd" = {}
"omni.ui" = {}
"omni.kit.window.property" = {}
"omni.kit.property.usd" = {}

[[python.module]]
name = "omni.kit.property.geometry"

[[test]]
args = [
    "--/app/window/dpiScaleOverride=1.0",
    "--/app/window/scaleToMonitor=false",
]

dependencies = [
    "omni.kit.mainwindow",
    "omni.kit.material.library", # for omni_pbr...
    "omni.kit.window.stage",
    "omni.kit.ui_test",
    "omni.kit.test_suite.helpers"
]

stdoutFailPatterns.exclude = [
    "*PrimVarCommand: cannot set value as * is type int and expected type is bool*",
    "*PrimVarCommand: cannot set value as * is type bool and expected type is int*"
]

[documentation]
pages = ["docs/Overview.md", "docs/CHANGELOG.md", "docs/SETTINGS.md", "docs/commands_api.md", "docs/USAGE_PYTHON.md"]
