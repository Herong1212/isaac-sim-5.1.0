#usda 1.0
(
    "Metrics Assembler Schema"
    subLayers = [
        @usdGeom/schema.usda@,
    ]
)

over "GLOBAL" (
    customData = {
        string libraryName      = "MetricsAssembler"
        string libraryPath      = "./"
        string libraryPrefix = "Omni"
    }
)
{
}
