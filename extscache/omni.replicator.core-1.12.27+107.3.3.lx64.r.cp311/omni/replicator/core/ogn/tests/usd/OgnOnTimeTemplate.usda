#usda 1.0
(
    doc ="""Generated from node description file OgnOnTime.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_OgnOnTime" (
        docs="""Triggers at the specified time interval. Note that the graph will run asynchronously to the new frame event"""
    )
    {
        token node:type = "omni.replicator.core.OgnOnTime"
        int node:typeVersion = 3

        # 6 attributes
        custom float inputs:interval = 1 (
            docs="""Trigger interval, in seconds."""
        )
        custom uint inputs:maxExecs = 0 (
            docs="""Number of sequences triggered before stopping. If 0, continue indefinitely."""
        )
        custom bool inputs:resetPhysics = true (
            docs="""If True, reset physics simulation on trigger."""
        )
        custom uint64 inputs:rtSubframes = 16 (
            docs="""Determines how many subframes to render in RealTime render mode on each trigger
            to reduce artifacts caused by sudden scene changes."""
        )
        custom bool inputs:run = false (
            docs="""Run"""
        )
        custom bool inputs:triggerOnLastFrame = false (
            docs="""If True, trigger on the frame before the interval time is reached."""
        )

        # 5 attributes
        custom int outputs:execCounts (
            docs="""The number of times the trigger has executed."""
        )
        custom uint outputs:execOut (
            docs="""Output Execution"""
        )
        custom uint64 outputs:referenceTimeDenominator (
            docs="""Reference time represented as a rational number : denominator"""
        )
        custom int64 outputs:referenceTimeNumerator (
            docs="""Reference time represented as a rational number : numerator"""
        )
        custom int outputs:time (
            docs="""The current physx simulation time."""
        )
    }
}
