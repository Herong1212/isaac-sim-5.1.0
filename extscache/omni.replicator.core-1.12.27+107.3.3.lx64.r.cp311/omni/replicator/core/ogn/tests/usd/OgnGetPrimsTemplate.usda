#usda 1.0
(
    doc ="""Generated from node description file OgnGetPrims.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_OgnGetPrims" (
        docs="""This node searches the stage by path and returns a list of prims."""
    )
    {
        token node:type = "omni.replicator.core.OgnGetPrims"
        int node:typeVersion = 1

        # 10 attributes
        custom bool inputs:cachePrims = true (
            docs="""If set to True, the stage is parsed only once."""
        )
        custom uint inputs:execIn (
            docs="""exec"""
        )
        custom bool inputs:ignoreCase = true (
            docs="""If set to True, use case-insensitive matching."""
        )
        custom string inputs:pathMatch = "" (
            docs="""The path substring to match"""
        )
        custom string inputs:pathPattern = "" (
            docs="""The RegEx (Regular Expression) path pattern to match"""
        )
        custom string inputs:pathPatternExclusion = "" (
            docs="""The RegEx (Regular Expression) path pattern to ignore"""
        )
        custom token[] inputs:primTypes = [] (
            docs="""List of prim types to include"""
        )
        custom token[] inputs:primTypesExclusion = [] (
            docs="""List of prim types to ignore"""
        )
        custom token[] inputs:semantics = [] (
            docs="""Semantic type-value pairs of semantics to include"""
        )
        custom token[] inputs:semanticsExclusion = [] (
            docs="""Semantic type-value pairs of semantics to ignore"""
        )

        # 2 attributes
        custom uint outputs:execOut (
            docs="""exec"""
        )
        custom rel outputs:prims (
            docs="""Prim paths from search result."""
        )
    }
}
