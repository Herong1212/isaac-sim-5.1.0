#usda 1.0
(
    doc ="""Generated from node description file OgnSemanticSegmentation.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_SemanticSegmentation" (
        docs="""This node outputs the semantic segmentation."""
    )
    {
        token node:type = "omni.replicator.core.SemanticSegmentation"
        int node:typeVersion = 1

        # 13 attributes
        custom uint inputs:bufferSize = 0 (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom bool inputs:colorize = false (
            docs="""If true, convert semantic IDs into colors."""
        )
        custom uint inputs:exec (
            docs="""Trigger"""
        )
        custom uint inputs:height = 0 (
            docs="""Shape of the data"""
        )
        custom uint[] inputs:ids = [] (
            docs="""Unoccluded semantic u ids (or color, if `colorize` is set to True)."""
        )
        custom int inputs:instanceSegmentationCudaDeviceIndex = -1 (
            docs="""Index of the device where the data lives (-1 for host data) for instance segmentationSD"""
        )
        custom uint64 inputs:instanceSegmentationPtr = 0 (
            docs="""Pointer to the raw instance segmetation data"""
        )
        custom int2 inputs:instanceSegmentationStrides = (0, 0) (
            docs="""Strides (in bytes) for InstanceSegmentationSD."""
        )
        custom token[] inputs:labels = [] (
            docs="""Prim path of the prim."""
        )
        custom string inputs:mapping = "{}" (
            docs="""Optional mapping enforcing semantics to specific IDs or colors."""
        )
        custom token inputs:semanticFilterName = "" (
            docs="""name of Semantic Filter to use"""
        )
        custom token[] inputs:semantics = [] (
            docs="""Semantic labels that correspond to the ids."""
        )
        custom uint inputs:width = 0 (
            docs="""Shape of the data"""
        )

        # 12 attributes
        custom bool outputs:_legacyIDs = true (
            docs="""If True, ids will be converted to strings for backwards compatibility"""
        )
        custom uint outputs:bufferSize (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom int outputs:cudaDeviceIndex = -1 (
            docs="""Index of the device where the data lives (-1 for host data)"""
        )
        custom uint64 outputs:dataPtr = 0 (
            docs="""Pointer to the raw data (host pointer)"""
        )
        custom int[] outputs:dataShape (
            docs="""Desired dimensions of output array"""
        )
        custom token outputs:dataType = "uint8" (
            docs="""Defines the data type"""
        )
        custom uint outputs:exec (
            docs="""Trigger"""
        )
        custom uint outputs:height (
            docs="""Shape of the data"""
        )
        custom uint[] outputs:ids (
            docs="""Unoccluded semantic u ids (or color, if `colorize` is set to True)."""
        )
        custom token[] outputs:labels (
            docs="""Prim path of the prim."""
        )
        custom int2 outputs:strides (
            docs="""Strides (in bytes)."""
        )
        custom uint outputs:width (
            docs="""Shape of the data"""
        )
    }
}
