#usda 1.0
(
    doc ="""Generated from node description file OgnRefTimeGate.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_OgnRefTimeGate" (
        docs="""Gate that passes execution only if the reference time is within the list of valid times."""
    )
    {
        token node:type = "omni.replicator.core.OgnRefTimeGate"
        int node:typeVersion = 1

        # 5 attributes
        custom bool inputs:enabled = false (
            docs="""If false, execution passes through the gate unchecked."""
        )
        custom uint inputs:exec (
            docs="""Execution in"""
        )
        custom uint64 inputs:referenceTimeDenominator = 0 (
            docs="""Reference time represented as a rational number : denominator"""
        )
        custom int64 inputs:referenceTimeNumerator = 0 (
            docs="""Reference time represented as a rational number : numerator"""
        )
        custom int2[] inputs:simTimesToWrite = [] (
            docs="""Sim times at which to allow further graph execution. Represented as numerator, denominator tuples. Must be in reduced form."""
        )

        # 3 attributes
        custom uint outputs:exec (
            docs="""Execution out"""
        )
        custom uint64 outputs:referenceTimeDenominator (
            docs="""Reference time represented as a rational number : denominator"""
        )
        custom int64 outputs:referenceTimeNumerator (
            docs="""Reference time represented as a rational number : numerator"""
        )
    }
}
