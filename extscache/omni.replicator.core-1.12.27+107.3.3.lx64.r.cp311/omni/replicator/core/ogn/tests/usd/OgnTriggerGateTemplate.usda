#usda 1.0
(
    doc ="""Generated from node description file OgnTriggerGate.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_OgnTriggerGate" (
        docs="""Gate that passes execution only once per sync value and only when both exec and triggerExec signals are received."""
    )
    {
        token node:type = "omni.replicator.core.OgnTriggerGate"
        int node:typeVersion = 1

        # 3 attributes
        custom uint inputs:exec (
            docs="""Execution in"""
        )
        custom uint64 inputs:syncValue = 0 (
            docs="""Value of the synchronization reference."""
        )
        custom uint inputs:triggerExec (
            docs="""Trigger Execution inputs"""
        )

        # 2 attributes
        custom uint outputs:exec (
            docs="""Execution out"""
        )
        custom uint64 outputs:syncValue (
            docs="""Value of the synchronization reference."""
        )
    }
}
