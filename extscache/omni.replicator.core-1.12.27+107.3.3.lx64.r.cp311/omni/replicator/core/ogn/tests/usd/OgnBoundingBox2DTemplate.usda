#usda 1.0
(
    doc ="""Generated from node description file OgnBoundingBox2D.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_replicator_core_BoundingBox2D" (
        docs="""This node outputs the 2D bounding box data."""
    )
    {
        token node:type = "omni.replicator.core.BoundingBox2D"
        int node:typeVersion = 1

        # 12 attributes
        custom uint inputs:bufferSize = 0 (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom uint64 inputs:dataPtr = 0 (
            docs="""Pointer to the raw bbox data (host pointer)"""
        )
        custom uint inputs:exec (
            docs="""Trigger"""
        )
        custom uint64 inputs:filteredSemanticLabelTokenPtr = 0 (
            docs="""Array containing filtered semantics of numSemantics uint64_t representing the semantic label of the semantic prim"""
        )
        custom uint inputs:minSemanticIndex = 0 (
            docs="""Semantic index of the first semantic prim in the semantic arrays"""
        )
        custom uint inputs:numSemantics = 0 (
            docs="""Number of semantic prim in the semantic arrays"""
        )
        custom token[] inputs:primPaths = [] (
            docs="""Prim paths corresponding to each bounding box."""
        )
        custom token inputs:semanticFilterName = "" (
            docs="""name of Semantic Filter to use"""
        )
        custom uint64[] inputs:semanticLabelTokenPtrs = [] (
            docs="""Array containing for every input semantic filters the corresponding array pointer of numSemantics uint64_t representing the semantic label of the semantic prim"""
        )
        custom uint64 inputs:semanticMapPtr = 0 (
            docs="""Array pointer of numSemantics uint16_t containing the semantic index of the semantic prim first semantic prim parent"""
        )
        custom uint64 inputs:semanticOcclusionPtr = 0 (
            docs="""Pointer to the raw occlusion data (host pointer)"""
        )
        custom token[] inputs:semanticTypes = [] (
            docs="""Semantic Types to consider"""
        )

        # 9 attributes
        custom uint[] outputs:bboxIds (
            docs="""Identifier associated with bounding box, used to link different types of bounding boxes to the same (eg. 2D Tight and 3D)"""
        )
        custom uint outputs:bufferSize (
            docs="""Size (in bytes) of the buffer (0 if the input is a texture)"""
        )
        custom uchar[] outputs:data (
            docs="""Extent data of bounding box."""
        )
        custom uint outputs:exec (
            docs="""Trigger"""
        )
        custom uint outputs:height (
            docs="""Shape of the data"""
        )
        custom uint[] outputs:ids (
            docs="""Unoccluded semantic ids of the bounding box."""
        )
        custom token[] outputs:labels (
            docs="""Semantic labels of the bounding box."""
        )
        custom token[] outputs:primPaths (
            docs="""Prim paths corresponding to each bounding box."""
        )
        custom uint outputs:width (
            docs="""Shape of the data"""
        )
    }
}
