# RETARTGETING UI (BETA) [omni.anim.retarget]

The Retargeting UI provides and interface to setup and edit retargeting metadata including tags, pose, facing, and more. It also allows users to preview their retarget on a default or user-specified character.