# Core RTX Settings [omni.rtx.settings.core]

This extension builds the UI widgets to control all core RTX renderer settings, including render mode-specific settings, common settings and post processing settings.

All settings have explanatory tooltips which can be inspected by hovering with the mouse over the respective labels.
