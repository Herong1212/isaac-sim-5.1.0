#usda 1.0
(
    doc ="""Generated from node description file VizTranscoderRadar.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_sensors_nv_radar_VizTranscoderRadar" (
        docs="""Receives Radar data and prepares a buffer for it from the buffer manager, and passes it to output"""
    )
    {
        token node:type = "omni.sensors.nv.radar.VizTranscoderRadar"
        int node:typeVersion = 1

        # 12 attributes
        custom int inputs:colorCode = 4 (
            docs="""0 - constant, 1 - range, 2 - doppler, 3 - azimuth, 4 - elevation, 5 - rcs, 6 - semantic, 7 - material, 8 - instance"""
        )
        custom uint64 inputs:cudaStreamIn = 0 (
            docs="""Cuda Stream Input"""
        )
        custom double inputs:hydraTime = 0.0 (
            docs="""hydra timestamp input as double in [ns]"""
        )
        custom uint64 inputs:in = 0 (
            docs="""Source Buffer"""
        )
        custom bool inputs:lastNode = false (
            docs="""true if the viz transcoder is last node in pipeline and needs to signal async work termination"""
        )
        custom int inputs:maxNumScans = 2 (
            docs="""max number of scans to accumulate"""
        )
        custom uint64 inputs:meta = 0 (
            docs="""Source Meta Buffer"""
        )
        custom int inputs:sendDataId = 0 (
            docs="""Send data buffer id to distinguish data on receiver side"""
        )
        custom float[] inputs:sensorMount6Dpose = [] (
            docs="""sensor mounting pose in vehicle coordinates as (x, y, z, roll, pitch, yaw)"""
        )
        custom double inputs:simTime = 0.0 (
            docs="""simTime timestamp input as double in [ns]"""
        )
        custom uint64 inputs:targetPID = 0 (
            docs="""Target process ID to send data stream to"""
        )
        custom float inputs:vlineScale = 1.0 (
            docs="""scale of velocity lines"""
        )
    }
}
