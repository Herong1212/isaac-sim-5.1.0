# Slider bar widget

To use:

```
from omni.kit.widget.sliderbar import SliderBar

def build_sliderbar():
    width = 200
    start = 5
    end = 20
    current = 13
    self._slider = SliderBar(200, 5, 20, 13)

```