# omni.timeline.live_session

## Python timeline live session

This extension provides timeline synchronization for Live Session. 

The timeline is controlled by a single user at a time (the "Presenter"), which role may be passed to another user dynamically. For all other users in the session (the "Listeners"), timeline controls are disabled.