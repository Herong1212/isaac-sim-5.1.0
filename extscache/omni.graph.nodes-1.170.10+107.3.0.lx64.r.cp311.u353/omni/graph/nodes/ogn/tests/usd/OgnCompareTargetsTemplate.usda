#usda 1.0
(
    doc ="""Generated from node description file OgnCompareTargets.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_nodes_CompareTargets" (
        docs="""Outputs the value of a comparison operation on target arrays."""
    )
    {
        token node:type = "omni.graph.nodes.CompareTargets"
        int node:typeVersion = 1

        # 4 attributes
        custom rel inputs:a (
            docs="""Input A"""
        )
        custom rel inputs:b (
            docs="""Input B"""
        )
        custom bool inputs:compareEach = false (
            docs="""If true, compare each array per-element.
            If false, compare the entire array and output a single value."""
        )
        custom token inputs:operation = "==" (
            docs="""The comparison operation to perform (==,!=))"""
        )

        # 1 attribute
        custom token outputs:result (
            docs="""The result of the comparison operation"""
        )
    }
}
