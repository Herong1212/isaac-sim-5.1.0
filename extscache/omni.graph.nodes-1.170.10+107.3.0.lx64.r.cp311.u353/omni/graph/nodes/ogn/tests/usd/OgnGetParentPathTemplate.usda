#usda 1.0
(
    doc ="""Generated from node description file OgnGetParentPath.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_nodes_GetParentPath" (
        docs="""Generates a parent path token from another path token. (ex. /World/Cube -> /World)"""
    )
    {
        token node:type = "omni.graph.nodes.GetParentPath"
        int node:typeVersion = 1

        # 1 attribute
        custom token inputs:path (
            docs="""One or more path tokens to compute a parent path from. (ex. /World/Cube)"""
        )

        # 1 attribute
        custom token outputs:parentPath (
            docs="""Parent path token (ex. /World)"""
        )

        # 1 attribute
        custom token state:path (
            docs="""Snapshot of previously seen path"""
        )
    }
}
