#usda 1.0
(
    doc ="""Generated from node description file OgnSelectTargetIf.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_nodes_SelectTargetIf" (
        docs="""Selects a target from the given inputs based on a boolean condition. If condition is a scalar, result will either be "ifTrue" or "ifFalse". If it is an array, select between each input element-wise (if arrays are the same length)."""
    )
    {
        token node:type = "omni.graph.nodes.SelectTargetIf"
        int node:typeVersion = 1

        # 3 attributes
        custom token inputs:condition (
            docs="""The boolean condition used to select between "ifTrue" and "ifFalse".
            If a scalar, result will either be "ifTrue" or "ifFalse".
            If an array, select between each input element-wise (if arrays are the same length)."""
        )
        custom rel inputs:ifFalse (
            docs="""The targets if condition is "False"."""
        )
        custom rel inputs:ifTrue (
            docs="""The targets if condition is "True"."""
        )

        # 1 attribute
        custom rel outputs:result (
            docs="""The selected targets from ifTrue and ifFalse"""
        )
    }
}
