#usda 1.0
(
    doc ="""Generated from node description file OgnClearVariantSelection.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_nodes_ClearVariantSelection" (
        docs="""This node will clear the variant selection of the prim on the layer defined in "Layer Identifier". Final variant selection will be determined by layer composition below the targeted layer. In a single layer stage, this will be the fallback variant defined in your variantSet."""
    )
    {
        token node:type = "omni.graph.nodes.ClearVariantSelection"
        int node:typeVersion = 3

        # 5 attributes
        custom uint inputs:execIn (
            docs="""Signal to the graph that this node is ready to be executed."""
        )
        custom token inputs:layerIdentifier = "" (
            docs="""Identifier of the USD layer to clear data from.
            Identifier can be empty, "<Session Layer>", "<Root Layer>" or the identifier of a sublayer.
            If empty or invalid, data will be cleared from the current layer."""
        )
        custom rel inputs:prim (
            docs="""The prim with the variantSet"""
        )
        custom bool inputs:setVariant = false (
            docs="""Clears the variant selection when finished rather than writing to the attribute values"""
        )
        custom token inputs:variantSetName = "" (
            docs="""The variantSet name"""
        )

        # 1 attribute
        custom uint outputs:execOut (
            docs="""Signal to the graph that execution can continue downstream."""
        )

        # 1 attribute
        custom token state:layerIdentifier (
            docs="""The prefetched layer identifier."""
        )
    }
}
