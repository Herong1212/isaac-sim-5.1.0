#usda 1.0
(
    doc ="""Generated from node description file OgnExitApplication.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_action_ExitApplication" (
        docs="""Immediately exits the application. Uses a fast shutdown to avoid any cleanup that would otherwise be triggered."""
    )
    {
        token node:type = "omni.graph.action.ExitApplication"
        int node:typeVersion = 1

        # 1 attribute
        custom uint inputs:execIn (
            docs="""Signal to the graph that this node is ready to be executed, exiting the application."""
        )
    }
}
