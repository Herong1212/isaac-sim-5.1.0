<svg style="max-width:100%; height:auto; max-height:100%;" id="efGraphTraversalAnim05"
     xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" overflow="hidden" width="1093.4"
     height="679.47" viewBox="0 0 1540 957">
      <rect x="0" y="0" width="100%" height="100%" fill="#FFFFFF" />

      <!--Execution Graph-->
      <rect x="2" y="98" width="1536" height="720" stroke="#000000" stroke-width="4" stroke-miterlimit="8"
            fill="#FFFFFF" />
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="400"
            font-size="16" transform="translate(11.6 118)" fill="#000000">Execution Graph</text>
      <path d="M144 276.002C144 216.354 192.354 168 252.002 168L1500 168C1559.65 168 1608 216.354 1608 276.002L1608 707.998C1608 767.646 1559.65 816 1500 816L252.002 816C192.354 816 144 767.646 144 707.998Z"
            stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
            transform="translate(-94 -22)" />
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="400"
            font-size="16" transform="translate(131.6 166)" fill="#000000">NodeGraphDef</text>
      <text class="noselect" font-family="Cambria Math,Cambria Math_MSFontService,sans-serif" font-weight="400"
            font-size="16" transform="translate(247.58 166)" fill="#000000">𝐺</text>
      <text class="noselect" font-family="Cambria Math,Cambria Math_MSFontService,sans-serif" font-weight="400"
            font-size="12" transform="translate(257.28 169)" fill="#000000">1</text>
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="400"
            font-size="16" transform="translate(268.946 166)" fill="#000000">has associated executor type</text>

      <!--Edges-->
      <g class="edges5" id="edges5">
            <g class="edges5" id="ab5">
                  <path d="M240.894 238.211 351.95 293.739 350.161 297.317 239.106 241.789ZM351.95 289.267 360 300 346.584 300Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="ac5">
                  <path d="M234.775 256.078 358.527 494.234 354.978 496.078 231.225 257.922ZM361.154 490.615 361.363 504.029 350.506 496.148Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="ad5">
                  <path d="M217.88 263.319 358.473 651.251 354.712 652.614 214.12 264.681ZM361.552 648.007 360 661.333 350.27 652.096Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="be5">
                  <path d="M4.37444e-06-2 230-1.9995 230 2.0005-4.37444e-06 2ZM228.001-5.9995 240.001 0.000524934 228.001 6.0005Z"
                        fill="#000000" transform="matrix(1 0 0 -1 314 278.001)" />
            </g>
            <g class="edges5" id="cf5">
                  <path d="M403.227 519.421 497.027 592.314 494.572 595.473 400.773 522.579ZM497.902 587.929 503.696 600.03 490.539 597.404Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="df5">
                  <path d="M1.07678-1.68539 88.6499 54.2641 86.4964 57.6349-1.07678 1.68539ZM89.1181 49.8165 96.0001 61.3333 82.6574 59.9288Z"
                        fill="#000000" transform="matrix(1 0 0 -1 314 639.333)" />
            </g>
            <g class="edges5" id="eg5">
                  <path d="M1056.7 298.125 1486.35 457.673 1484.96 461.423 1055.3 301.875ZM1485.87 453.228 1495.03 463.03 1481.69 464.477Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="fd5">
                  <path d="M0 2-23.9999 2-21.9999 0-21.9999 468-23.9999 466 72.6665 466 72.6665 470-25.9999 470-25.9999-2 0-2ZM70.6665 462 82.6665 468 70.6665 474Z"
                        fill="#000000" transform="matrix(-6.12323e-17 -1 -1 6.12323e-17 758 745.666)" />
            </g>
            <g class="edges5" id="fg5">
                  <path d="M0.659386-1.88818 286.248 97.8445 284.929 101.621-0.659386 1.88818ZM285.678 93.4088 295.029 103.03 281.722 104.738Z"
                        fill="#000000" transform="matrix(1 0 0 -1 1106 578.03)" />
            </g>
            <g class="edges5" id="h1i15">
                  <path d="M720 315 806 315 806 319 720 319ZM804 311 816 317 804 323Z" fill="#000000"
                        transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="i1j15">
                  <path d="M864 315 948.667 315 948.667 319 864 319ZM946.667 311 958.667 317 946.667 323Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="j1i15">
                  <path d="M985.667 340 985.667 366 839 366 839 351.333 843 351.333 843 364 841 362 983.667 362 981.667 364 981.667 340ZM835 353.333 841 341.333 847 353.333Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="kl5">
                  <path d="M570.712 543.965 620.568 626.437 617.144 628.506 567.288 546.035ZM622.956 622.656 624.029 636.029 612.687 628.864Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="lm5">
                  <path d="M2.91628e-06-2 62.0004-1.99991 62.0004 2.00009-2.91628e-06 2ZM60.0004-5.99991 72.0004 0.000104987 60.0004 6.00009Z"
                        fill="#000000" transform="matrix(1 0 0 -1 578 614)" />
            </g>
            <g class="edges5" id="h2i25">
                  <path d="M816 651 902 651 902 655 816 655ZM900 647 912 653 900 659Z" fill="#000000"
                        transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="i2j25">
                  <path d="M960 651 1044.67 651 1044.67 655 960 655ZM1042.67 647 1054.67 653 1042.67 659Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
            <g class="edges5" id="j2i25">
                  <path d="M1081.67 676 1081.67 702 935 702 935 687.333 939 687.333 939 700 937 698 1079.67 698 1077.67 700 1077.67 676ZM931 689.333 937 677.333 943 689.333Z"
                        fill="#000000" transform="translate(-94 -22)" />
            </g>
      </g>

      <!--Nodes (with corresponding visit counters)-->
      <g class="nodes5" id="nodes5">
            <g class="nodes5" id="a5" is-filled="false" successful-past-visit="false">
                  <path d="M192 240C192 226.745 202.745 216 216 216 229.255 216 240 226.745 240 240 240 253.255 229.255 264 216 264 202.745 264 192 253.255 192 240Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                        transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(113 229)" fill="#000000">a</text>
            </g>
            <g class="nodes5" id="b5" is-filled="true" successful-past-visit="false">
                  <path d="M360 300C360 286.745 370.745 276 384 276 397.255 276 408 286.745 408 300 408 313.255 397.255 324 384 324 370.745 324 360 313.255 360 300Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(280.25 289)">b</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(277.083 238)">0/1</text>
            </g>
            <g class="nodes5" id="c5" is-filled="true" successful-past-visit="false">
                  <path d="M361 504C361 490.745 371.745 480 385 480 398.255 480 409 490.745 409 504 409 517.255 398.255 528 385 528 371.745 528 361 517.255 361 504Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(282.834 493)">c</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(277.083 442)">0/1</text>
            </g>
            <g class="nodes5" id="d5" is-filled="true" successful-past-visit="false">
                  <path d="M360 661C360 647.745 370.745 637 384 637 397.255 637 408 647.745 408 661 408 674.255 397.255 685 384 685 370.745 685 360 674.255 360 661Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(280.25 650)">d</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(277.083 598)">0/1</text>
            </g>
            <g class="nodes5" id="e5" is-filled="false" successful-past-visit="false">
                  <path d="M648 244.001C648 228.536 660.536 216 676 216L1028 216C1043.46 216 1056 228.536 1056 244.001L1056 355.999C1056 371.464 1043.46 384 1028 384L676 384C660.536 384 648 371.464 648 355.999Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-opacity="0"
                        fill-rule="evenodd" transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(563.6 181)" fill="#000000">Node e</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(587.6 229)" fill="#000000">NodeGraphDef
                        X</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(732.333 181)">0/1</text>
            </g>
            <g class="nodes5" id="f5" is-filled="false" successful-past-visit="false">
                  <path d="M504 488.001C504 457.073 529.073 432 560.001 432L1144 432C1174.93 432 1200 457.073 1200 488.001L1200 711.999C1200 742.927 1174.93 768 1144 768L560.001 768C529.073 768 504 742.927 504 711.999Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-opacity="0"
                        fill-rule="evenodd" transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(419.6 397)" fill="#000000">Node f</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(443.6 451)" fill="#000000">NodeGraphDef
                        Y</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(732.333 397)">0/1</text>
            </g>
            <g class="nodes5" id="g5" is-filled="true" successful-past-visit="false">
                  <path d="M1488 480C1488 466.745 1498.75 456 1512 456 1525.25 456 1536 466.745 1536 480 1536 493.255 1525.25 504 1512 504 1498.75 504 1488 493.255 1488 480Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(1408.25 469)">g</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(1405.08 406)">0/1</text>
            </g>
            <g class="nodes5" id="h15" is-filled="false" successful-past-visit="false">
                  <path d="M672 317C672 303.745 682.745 293 696 293 709.255 293 720 303.745 720 317 720 330.255 709.255 341 696 341 682.745 341 672 330.255 672 317Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                        transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(592.334 305)" fill="#000000">h</text>
            </g>
            <g class="nodes5" id="i15" is-filled="true" successful-past-visit="false">
                  <path d="M816 317C816 303.745 826.745 293 840 293 853.255 293 864 303.745 864 317 864 330.255 853.255 341 840 341 826.745 341 816 330.255 816 317Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(741.667 305)">i</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(733.083 262)">0/1</text>
            </g>
            <g class="nodes5" id="j15" is-filled="true" successful-past-visit="false">
                  <path d="M959 317C959 303.745 969.745 293 983 293 996.255 293 1007 303.745 1007 317 1007 330.255 996.255 341 983 341 969.745 341 959 330.255 959 317Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(884.333 305)">j</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(877.083 262)">0/1</text>
            </g>
            <g class="nodes5" id="k5" is-filled="false" successful-past-visit="false">
                  <path d="M528 528C528 514.745 538.745 504 552 504 565.255 504 576 514.745 576 528 576 541.255 565.255 552 552 552 538.745 552 528 541.255 528 528Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                        transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(448.666 517)" fill="#000000">k</text>
            </g>
            <g class="nodes5" id="l5" is-filled="true" successful-past-visit="false">
                  <path d="M624 636C624 622.745 634.745 612 648 612 661.255 612 672 622.745 672 636 672 649.255 661.255 660 648 660 634.745 660 624 649.255 624 636Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(549.666 625)">l</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(541.083 574)">0/1</text>
            </g>
            <g class="nodes5" id="m5" is-filled="false" successful-past-visit="false">
                  <path d="M744 580C744 564.536 756.536 552 772 552L1124 552C1139.46 552 1152 564.536 1152 580L1152 691.999C1152 707.464 1139.46 720 1124 720L772 720C756.536 720 744 707.464 744 691.999Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-opacity="0"
                        fill-rule="evenodd" transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(659.6 517)" fill="#000000">Node m</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(683.6 565)" fill="#000000">NodeGraphDef
                        X</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(828.333 517)">0/1</text>
            </g>
            <g class="nodes5" id="h25" is-filled="false" successful-past-visit="false">
                  <path d="M768 653C768 639.745 778.745 629 792 629 805.255 629 816 639.745 816 653 816 666.255 805.255 677 792 677 778.745 677 768 666.255 768 653Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                        transform="translate(-94 -22)" />
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(688.333 641)" fill="#000000">h</text>
            </g>
            <g class="nodes5" id="i25" is-filled="true" successful-past-visit="false">
                  <path d="M912 653C912 639.745 922.745 629 936 629 949.255 629 960 639.745 960 653 960 666.255 949.255 677 936 677 922.745 677 912 666.255 912 653Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(837.667 641)">i</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(829.083 598)">0/1</text>
            </g>
            <g class="nodes5" id="j25" is-filled="true" successful-past-visit="false">
                  <path d="M1055 653C1055 639.745 1065.75 629 1079 629 1092.25 629 1103 639.745 1103 653 1103 666.255 1092.25 677 1079 677 1065.75 677 1055 666.255 1055 653Z"
                        stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
                        transform="translate(-94 -22)" />
                  <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="32" transform="translate(980.33 641)">j</text>
                  <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                        font-weight="700" font-size="16" transform="translate(973.08 598)">0/1</text>
            </g>
      </g>

      <!--Miscellaneous Elements/Definitions (left-to-right)-->
      <text class="noselect" fill="#7F7F7F" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="400" font-size="16" transform="translate(0 40)">Root is a special node used to</text>
      <text class="noselect" fill="#7F7F7F" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="400" font-size="16" transform="translate(0 59)">discover the entire topology.</text>
      <path d="M336 60C336 40.1177 352.118 24 372 24 391.882 24 408 40.1177 408 60 408 79.8822 391.882 96 372 96 352.118 96 336 79.8822 336 60Z"
            stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
            transform="translate(-94 -22)" />
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="700"
            fill="#000000" font-size="12" transform="translate(264.417 42)">Root</text>
      <path d="M1446 60C1446 40.1177 1462.12 24 1482 24 1501.88 24 1518 40.1177 1518 60 1518 79.8822 1501.88 96 1482 96 1462.12 96 1446 79.8822 1446 60Z"
            stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill-rule="evenodd" fill="#000000"
            transform="translate(-94 -22)" />
      <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="700" font-size="12" transform="translate(1373 35)">Node</text>
      <text class="noselect" fill="#FFFFFF" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="700" font-size="12" transform="translate(1377.67 50)">Def</text>
      <path d="M1536 36.0002C1536 29.3727 1541.37 24 1548 24L1620 24C1626.63 24 1632 29.3727 1632 36.0002L1632 83.9998C1632 90.6273 1626.63 96 1620 96L1548 96C1541.37 96 1536 90.6273 1536 83.9998Z"
            stroke="#000000" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
            transform="translate(-94 -22)" />
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="700"
            font-size="12" transform="translate(1457.67 35)" fill="#000000">NodeGraph</text>
      <text class="noselect" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif" font-weight="700"
            font-size="12" transform="translate(1479.67 50)" fill="#000000">Def</text>
      <text class="noselect" fill="#7F7F7F" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="400" font-size="16" transform="translate(1145.6 22)">Each node in topology has</text>
      <text class="noselect" fill="#7F7F7F" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="400" font-size="16" transform="translate(1145.6 41)">one of two execution</text>
      <text class="noselect" fill="#7F7F7F" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
            font-weight="400" font-size="16" transform="translate(1145.6 60)">definition types.</text>

      <!--Buttons-->
      <rect x="614" y="859" width="312" height="96" stroke="#000000" stroke-width="4" stroke-miterlimit="8"
            fill="#F8CBAD" />
      <g onmouseover="onMouseOver5(this)" onclick=" onClickGoToStart5(this)" onmousedown="onMouseDown5(this)"
         onmouseup="onMouseUp5(this)" onmouseleave="onMouseLeave5(this)">
            <path d="M732 891C732 886.582 735.582 883 740 883L772 883C776.418 883 780 886.582 780 891L780 923C780 927.418 776.418 931 772 931L740 931C735.582 931 732 927.418 732 923Z"
                  stroke="#C55A11" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                  transform="translate(-94 0)" />
            <text class=" noselect" fill="#C55A11" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                  font-weight="700" font-size="16" transform="translate(652.333 912)">&lt;&lt;</text>
      </g>
      <g onmouseover="onMouseOver5(this)" onclick=" onClickBackward5(this)" onmousedown="onMouseDown5(this)"
         onmouseup="onMouseUp5(this)" onmouseleave="onMouseLeave5(this)">
            <path d="M804 891C804 886.582 807.582 883 812 883L844 883C848.418 883 852 886.582 852 891L852 923C852 927.418 848.418 931 844 931L812 931C807.582 931 804 927.418 804 923Z"
                  stroke="#C55A11" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                  transform="translate(-94 0)" />
            <text class="noselect" fill="#C55A11" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                  font-weight="700" font-size="16" transform="translate(729.166 912)">&lt;</text>
      </g>
      <g onmouseover="onMouseOver5(this)" onclick=" onClickForward5(this)" onmousedown="onMouseDown5(this)"
         onmouseup="onMouseUp5(this)" onmouseleave="onMouseLeave5(this)">
            <path d="M876 891C876 886.582 879.582 883 884 883L916 883C920.418 883 924 886.582 924 891L924 923C924 927.418 920.418 931 916 931L884 931C879.582 931 876 927.418 876 923Z"
                  stroke="#C55A11" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                  transform="translate(-94 0)" />
            <text class="noselect" fill="#C55A11" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                  font-weight="700" font-size="16" transform="translate(801.166 912)">&gt;</text>
      </g>
      <g onmouseover="onMouseOver5(this)" onclick=" onClickGoToEnd5(this)" onmousedown="onMouseDown5(this)"
         onmouseup="onMouseUp5(this)" onmouseleave="onMouseLeave5(this)">
            <path d="M948 891C948 886.582 951.582 883 956 883L988 883C992.418 883 996 886.582 996 891L996 923C996 927.418 992.418 931 988 931L956 931C951.582 931 948 927.418 948 923Z"
                  stroke="#C55A11" stroke-width="4" stroke-miterlimit="8" fill="#FFFFFF" fill-rule="evenodd"
                  transform="translate(-94 0)" />
            <text class="noselect" fill="#C55A11" font-family="NVIDIA Sans,NVIDIA Sans_MSFontService,sans-serif"
                  font-weight="700" font-size="16" transform="translate(868.333 912)">&gt;&gt;</text>
      </g>

      <style id="dynamicStyles5">
            /*Center the SVG*/
            #efGraphTraversalAnim05 {
                  margin-left: auto;
                  margin-right: auto;
                  display: block;
            }

            .noselect {
                  /* iOS Safari */
                  -webkit-touch-callout: none;
                  /* Safari */
                  -webkit-user-select: none;
                  /* Konqueror HTML */
                  -khtml-user-select: none;
                  /* Old versions of Firefox */
                  -moz-user-select: none;
                  /* Internet Explorer/Edge */
                  -ms-user-select: none;
                  /* Non-prefixed version, currently supported by Chrome, Edge, Opera and Firefox */
                  user-select: none;
            }
      </style>

      <script type="text/javascript">
            //<![CDATA[

            let dynamicStyle5 = document.getElementById("dynamicStyles5");

            // Dynamic CSS. Allows us to alter certain CSS parameters
            // at runtime by completely rebuilding it.
            function createCssDynamicTextContent5(nodeIdx, edgeIdx, colors, fillMode, animDir) {
                  return `
                        :root {
                              --node-fill-default-color: ${colors["nodeDefaultFillColor"]};
                              --node-stroke-default-color: ${colors["nodeDefaultStrokeColor"]};
                              --edge-default-color: ${colors["edgeDefaultColor"]};
                              --visit-try-color: ${colors["visitTryColor"]};
                              --visit-success-color: ${colors["visitSuccessColor"]};
                              --visit-failure-color: ${colors["visitFailureColor"]};
                        }
      
                        /*Edge Animation Keyframes*/
                        @keyframes edge_visit_try {
                              0% {
                                    fill: var(--edge-default-color);
                              }
      
                              100% {
                                    fill: var(--visit-try-color);
                              }
                        }
      
                        @keyframes edge_visit_success {
                              0% {
                                    fill: var(--visit-try-color);
                              }
      
                              100% {
                                    fill: var(--visit-success-color);
                              }
                        }
      
                        @keyframes edge_visit_failure {
                              0% {
                                    fill: var(--visit-try-color);
                              }
      
                              100% {
                                    fill: var(--visit-failure-color);
                              }
                        }
      
                        /*Edge Animations*/
                        .edges5:nth-child(${edgeIdx}).visitTry>path {
                              animation: edge_visit_try 0.5s ease ${fillMode} ${animDir};
                        }
      
                        .edges5:nth-child(${edgeIdx}).visitSuccess>path {
                              animation: edge_visit_success 0.5s ease ${fillMode} ${animDir};
                        }
      
                        .edges5:nth-child(${edgeIdx}).visitFailure>path {
                              animation: edge_visit_failure 1s ease ${fillMode} ${animDir};
                        }
      
                        /*Node Animation Keyframes*/
                        @keyframes node_visit_try_already_visited {
                              0% {
                                    stroke: var(--visit-success-color);
                              }
                              100% {
                                    stroke: var(--visit-try-color);
                              }
                        }
      
                        @keyframes node_visit_try_never_visited {
                              0% {
                                    stroke: var(--node-stroke-default-color);
                              }
                              100% {
                                    stroke: var(--visit-try-color);
                              }
                        }
      
                        @keyframes filled_node_visit_success {
                              0% {
                                    fill: var(--node-fill-default-color);
                                    stroke: var(--visit-try-color);
                              }
      
                              100% {
                                    fill: var(--visit-success-color);
                                    stroke: var(--visit-success-color);
                              }
                        }
      
                        @keyframes unfilled_node_visit_success {
                              0% {
                                    stroke: var(--visit-try-color);
                              }
      
                              100% {
                                    stroke: var(--visit-success-color);
                              }
                        }
      
                        @keyframes unfilled_node_visit_success_text {
                              0% {
                                    fill: var(--node-stroke-default-color);
                              }
      
                              100% {
                                    fill: var(--visit-success-color);
                              }
                        }
      
                        @keyframes node_visit_failure_already_visited {
                              0% {
                                    stroke: var(--visit-try-color);
                              }
      
                              50% {
                                    stroke: var(--visit-failure-color);
                              }
      
                              100% {
                                    stroke: var(--visit-success-color);
                              }
                        }
      
                        @keyframes node_visit_failure_never_visited {
                              0% {
                                    stroke: var(--visit-try-color);
                              }
      
                              50% {
                                    stroke: var(--visit-failure-color);
                              }
      
                              100% {
                                    stroke: var(--node-stroke-default-color);
                              }
                        }
                        
                        /*Node Animations*/
                        .nodes5.visitTryAlreadyVisited:nth-child(${nodeIdx})>path {
                              animation: node_visit_try_already_visited 0.5s ease ${fillMode} ${animDir};
                        }
      
                        .nodes5.visitTryNeverVisited:nth-child(${nodeIdx})>path {
                              animation: node_visit_try_never_visited 0.5s ease ${fillMode} ${animDir};
                        }
      
                        .nodes5.filledVisitSuccess:nth-child(${nodeIdx})>path {
                              animation: filled_node_visit_success 0.5s ease ${fillMode} ${animDir};
                        }
     
                        /*Apply animation to the path that draws the circle*/
                        .nodes5.unfilledVisitSuccess:nth-child(${nodeIdx})>path {
                              animation: unfilled_node_visit_success 0.5s ease ${fillMode} ${animDir};
                        }
      
                        /*Apply animation to all other child elements of the node, which is just text.*/
                        .nodes5.unfilledVisitSuccess:nth-child(${nodeIdx})>text {
                              animation: unfilled_node_visit_success_text 0.5s ease ${fillMode} ${animDir};
                        }

                        .nodes5.visitSuccessWithPrevSuccesses:nth-child(${nodeIdx})>path {
                              animation: unfilled_node_visit_success 0.5s ease ${fillMode} ${animDir};
                        }
      
                        .nodes5.visitFailureAlreadyVisited:nth-child(${nodeIdx})>path {
                              animation: node_visit_failure_already_visited 1s ease ${fillMode} ${animDir};
                        }
      
                        .nodes5.visitFailureNeverVisited:nth-child(${nodeIdx})>path {
                              animation: node_visit_failure_never_visited 1s ease ${fillMode} ${animDir};
                        }

                        /*Center the SVG*/
                        #efGraphTraversalAnim05 {
                              margin-left: auto;
                              margin-right: auto;
                              display: block;
                        }
      
                        .noselect {
                              /* iOS Safari */
                              -webkit-touch-callout: none;
                              /* Safari */
                              -webkit-user-select: none;
                              /* Konqueror HTML */
                              -khtml-user-select: none;
                              /* Old versions of Firefox */
                              -moz-user-select: none;
                              /* Internet Explorer/Edge */
                              -ms-user-select: none;
                              /* Non-prefixed version, currently supported by Chrome, Edge, Opera and Firefox */
                              user-select: none;
                        }
                        `;
            }

            // Establishes the exact node/edge traversal ordering.
            const nodesAndEdgesOrdering5 = [
                  [["a5", "node", "try", "1"]],
                  [["a5", "node", "success", "1"]],
                  [["ab5", "edge", "try"], ["b5", "node", "try", "1"]],
                  [["ab5", "edge", "success"], ["b5", "node", "success", "1"]],
                  [["be5", "edge", "try"], ["e5", "node", "try", "1"]],
                  [["be5", "edge", "success"], ["e5", "node", "success", "1"]],
                  [["h15", "node", "try", "1"]],
                  [["h15", "node", "success", "1"]],
                  [["h1i15", "edge", "try"], ["i15", "node", "try", "1"]],
                  [["h1i15", "edge", "success"], ["i15", "node", "success", "1"]],
                  [["i1j15", "edge", "try"], ["j15", "node", "try", "1"]],
                  [["i1j15", "edge", "success"], ["j15", "node", "success", "1"]],
                  [["j1i15", "edge", "try"], ["i15", "node", "try", "2"]],
                  [["j1i15", "edge", "success"], ["i15", "node", "success", "2"]],
                  [["eg5", "edge", "try"], ["g5", "node", "try", "1"]],
                  [["eg5", "edge", "success"], ["g5", "node", "success", "1"]],
                  [["ac5", "edge", "try"], ["c5", "node", "try", "1"]],
                  [["ac5", "edge", "success"], ["c5", "node", "success", "1"]],
                  [["cf5", "edge", "try"], ["f5", "node", "try", "1"]],
                  [["cf5", "edge", "success"], ["f5", "node", "success", "1"]],
                  [["k5", "node", "try", "1"]],
                  [["k5", "node", "success", "1"]],
                  [["kl5", "edge", "try"], ["l5", "node", "try", "1"]],
                  [["kl5", "edge", "success"], ["l5", "node", "success", "1"]],
                  [["lm5", "edge", "try"], ["m5", "node", "try", "1"]],
                  [["lm5", "edge", "success"], ["m5", "node", "success", "1"]],
                  [["h25", "node", "try", "1"]],
                  [["h25", "node", "success", "1"]],
                  [["h2i25", "edge", "try"], ["i25", "node", "try", "1"]],
                  [["h2i25", "edge", "success"], ["i25", "node", "success", "1"]],
                  [["i2j25", "edge", "try"], ["j25", "node", "try", "1"]],
                  [["i2j25", "edge", "success"], ["j25", "node", "success", "1"]],
                  [["j2i25", "edge", "try"], ["i25", "node", "try", "2"]],
                  [["j2i25", "edge", "success"], ["i25", "node", "success", "2"]],
                  [["fd5", "edge", "try"], ["d5", "node", "try", "1"]],
                  [["fd5", "edge", "success"], ["d5", "node", "success", "1"]],
                  [["df5", "edge", "try"], ["f5", "node", "try", "2"]],
                  [["df5", "edge", "success"], ["f5", "node", "success", "2"]],
                  [["fg5", "edge", "try"], ["g5", "node", "try", "2"]],
                  [["fg5", "edge", "success"], ["g5", "node", "success", "2"]],
                  [["ad5", "edge", "try"], ["d5", "node", "try", "2"]],
                  [["ad5", "edge", "success"], ["d5", "node", "success", "2"]],
            ];

            // Node + edge colors.
            const nodeAndEdgeColors5 = {
                  "nodeDefaultFillColor": "#000000",
                  "nodeDefaultStrokeColor": "#000000",
                  "edgeDefaultColor": "#000000",
                  "visitTryColor": "#FFD100",
                  "visitSuccessColor": "#005593",
                  "visitFailureColor": "#BB1E10",
            };

            // Button state colors.
            const buttonDefaultColor5 = "#FFFFFF";
            const buttonHoverColor5 = "#BFBFBF";
            const buttonDownColor5 = "#7F7F7F";

            let cnt5 = -1;

            function onClickGoToStart5(buttonElement) {
                  // Reset counter to start.
                  cnt5 = -1;

                  // Reset all edges to their initial state color, and remove all anim-related classes.
                  const edges = document.getElementById("edges5");
                  for (const edge of edges.children) {
                        edge.classList.value = "edges5";
                        const path = edge.getElementsByTagName("path").item(0);
                        path.setAttribute("fill", nodeAndEdgeColors5["edgeDefaultColor"]);

                        // Listen for any animations that may have been playing that just
                        // ended and/or got cancelled by this action, and make sure to
                        // overwrite the final colors with the correct values after the
                        // fact.
                        edge.addEventListener("animationend", () => {
                              path.setAttribute("fill", nodeAndEdgeColors5["edgeDefaultColor"]);
                        }, { once: true });
                        edge.addEventListener("animationcancel", () => {
                              path.setAttribute("fill", nodeAndEdgeColors5["edgeDefaultColor"]);
                        }, { once: true });
                  }

                  // Reset all nodes to their initial state color, and remove all anim-related classes.
                  const nodes = document.getElementById("nodes5");
                  for (const node of nodes.children) {
                        node.classList.value = "nodes5";
                        const path = node.getElementsByTagName("path").item(0);
                        const textElements = node.getElementsByTagName("text");

                        if (node.getAttribute("is-filled") == "true") {
                              path.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);

                              // Reset the visit count text color.
                              const text = textElements.item(textElements.length - 1);
                              text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                        }
                        else {
                              path.setAttribute("fill", "#FFFFFF");

                              // Reset all text color.
                              for (let i = 0; i < textElements.length; ++i) {
                                    const text = textElements.item(i);
                                    text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                              }
                        }
                        path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                        node.setAttribute("successful-past-visit", "false");

                        // Reset the visit counter. Note that the visit counter element will always
                        // be the last element in a node since I've structured the svg in that manner
                        // (for convenience's sake).
                        if (textElements.length > 1) {
                              const visitCounter = textElements.item(textElements.length - 1);
                              let vcnt5 = visitCounter.textContent;
                              vcnt5 = vcnt5.split("/");
                              if (vcnt5.length == 2) {
                                    visitCounter.textContent = "0/" + vcnt5[1];
                              }
                        }

                        // Listen for any animations that may have been playing that just
                        // ended and/or got cancelled by this action, and make sure to
                        // overwrite the final colors with the correct values after the
                        // fact.
                        node.addEventListener("animationend", () => {
                              if (node.getAttribute("is-filled") == "true") {
                                    path.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);

                                    // Reset the visit count text color.
                                    const text = textElements.item(textElements.length - 1);
                                    text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                              }
                              else {
                                    path.setAttribute("fill", "#FFFFFF");

                                    // Reset all text color.
                                    for (let i = 0; i < textElements.length; ++i) {
                                          const text = textElements.item(i);
                                          text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                    }
                              }
                              path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                        }, { once: true });
                        node.addEventListener("animationcancel", () => {
                              if (node.getAttribute("is-filled") == "true") {
                                    path.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);

                                    // Reset the visit count text color.
                                    const text = textElements.item(textElements.length - 1);
                                    text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                              }
                              else {
                                    path.setAttribute("fill", "#FFFFFF");

                                    // Reset all text color.
                                    for (let i = 0; i < textElements.length; ++i) {
                                          const text = textElements.item(i);
                                          text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                    }
                              }
                              path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                        }, { once: true });
                  }
            }

            function onClickBackward5(buttonElement) {
                  if (cnt5 < 0) {
                        return;
                  }

                  // Get a list of the current elements that need to undergo animation.
                  const listOfElements = nodesAndEdgesOrdering5[cnt5];
                  let prevEdgeIdx = 0;

                  for (let i = 0; i < listOfElements.length; ++i) {
                        // Unpack the element attributes.
                        const elementAttrs = listOfElements[i];
                        const name = elementAttrs[0];
                        const type = elementAttrs[1];
                        const status = elementAttrs[2];

                        if (type == "edge") {
                              // Get the "edges5" element (which contains all edges), the actual current
                              // edge's html element (which is a child of the "edges5" element as well) and
                              // the corresponding path element.
                              const edges = document.getElementById("edges5");
                              const edge = document.getElementById(name);
                              const path = edge.getElementsByTagName("path").item(0);

                              // Rebuild the CSS stylesheet.
                              const edgeIdx = Array.prototype.indexOf.call(edges.children, edge) + 1;
                              prevEdgeIdx = edgeIdx;
                              dynamicStyle5.innerHTML = createCssDynamicTextContent5(0, edgeIdx, nodeAndEdgeColors5, "backwards", "reverse");

                              // Depending on the edge status, add the corresponding name
                              // to the class and set the colors to their final values
                              // after the animation finishes.

                              // When reversing from "try", we want to go back to the default edge color
                              // (since no single edge will be traversed more than once, so it's guaranteed
                              // that this is the first time that the current edge was iterated over).
                              if (status == "try") {
                                    edge.classList.add("visitTry");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["edgeDefaultColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["edgeDefaultColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                              // When reversing from "success", it's guaranteed that the previous state
                              // was "try" (i.e. we had to try and visit the element before being able to
                              // declare that visit a success).
                              else if (status == "success") {
                                    edge.classList.add("visitSuccess");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                              // When reversing from "failure", it's guaranteed that the previous state
                              // was "try" (i.e. we had to try and visit the element before being able to
                              // declare that visit a failure).
                              else if (status == "failure") {
                                    edge.classList.add("visitFailure");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                        }
                        else if (type == "node") {
                              // Get the "nodes5" element (which contains all nodes), the actual current
                              // nodes's html element (which is a child of the "nodes5" element as well) and
                              // the corresponding path element.
                              const nodes = document.getElementById("nodes5");
                              const node = document.getElementById(name);
                              const path = node.getElementsByTagName("path").item(0);
                              const textElements = node.getElementsByTagName("text");

                              // Rebuild the CSS stylesheet.
                              const nodeIdx = Array.prototype.indexOf.call(nodes.children, node) + 1;
                              dynamicStyle5.innerHTML = createCssDynamicTextContent5(nodeIdx, prevEdgeIdx, nodeAndEdgeColors5, "backwards", "reverse");

                              // Depending on the node status, add the corresponding name
                              // to the class and set the colors to their final values
                              // after the animation finishes.

                              // When reversing a "try", we need to check whether the current
                              // node has already been visited previously.
                              if (status == "try") {
                                    const visitCount = elementAttrs[3];

                                    // Decrement the visit counter immediately.
                                    if (textElements.length > 1) {
                                          const visitCounter = textElements.item(textElements.length - 1);
                                          let vcnt5 = visitCounter.textContent;
                                          vcnt5 = vcnt5.split("/");
                                          if (vcnt5.length == 2) {
                                                vcnt5[0] = String(Number(visitCount) - 1);
                                                visitCounter.textContent = vcnt5[0] + "/" + vcnt5[1];
                                          }
                                    }

                                    // If we have a visit counter to reach, then compare against
                                    // the denominator.
                                    if (textElements.length > 1) {
                                          const visitCounter = textElements.item(textElements.length - 1);
                                          let vcnt5 = visitCounter.textContent;
                                          vcnt5 = vcnt5.split("/");
                                          if (vcnt5[0] >= vcnt5[1]) {
                                                node.classList.add("visitTryAlreadyVisited");
                                          }
                                          else {
                                                node.classList.add("visitTryNeverVisited");
                                          }
                                    }
                                    // If no visit counter exists, then just check if this is
                                    // the first visit.
                                    else {
                                          if (visitCount != "1") {
                                                node.classList.add("visitTryAlreadyVisited");
                                          }
                                          else {
                                                node.classList.add("visitTryNeverVisited");
                                          }
                                    }

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    node.addEventListener("animationend", () => {
                                          if (textElements.length > 1) {
                                                const visitCounter = textElements.item(textElements.length - 1);
                                                let vcnt5 = visitCounter.textContent;
                                                vcnt5 = vcnt5.split("/");
                                                if (vcnt5[0] >= vcnt5[1]) {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                                }
                                          }
                                          else {
                                                if (visitCount != "1") {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                                }
                                          }

                                          node.classList.value = "nodes5";
                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          if (textElements.length > 1) {
                                                const visitCounter = textElements.item(textElements.length - 1);
                                                let vcnt5 = visitCounter.textContent;
                                                vcnt5 = vcnt5.split("/");
                                                if (vcnt5[0] >= vcnt5[1]) {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                                }
                                          }
                                          else {
                                                if (visitCount != "1") {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                                }
                                          }

                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                              // When reversing a "success", it's guaranteed that the previous state
                              // was "try" (i.e. we had to try and visit the element before being able to
                              // declare that visit a success). We do need to check if the node has been
                              // successfully visited in the past though (e.g. with the VisitAll strategy).
                              else if (status == "success") {
                                    const visitCount = elementAttrs[3];

                                    // Check if the node has been successfully visited in previous steps,
                                    // not counting the current step that's about to be reversed.
                                    if (textElements.length > 1) {
                                          const visitCounter = textElements.item(textElements.length - 1);
                                          let vcnt5 = visitCounter.textContent;
                                          vcnt5 = vcnt5.split("/");
                                          if (visitCount <= vcnt5[1]) {
                                                node.setAttribute("successful-past-visit", "false");
                                          }
                                    }
                                    else if (visitCount == "1") {
                                          node.setAttribute("successful-past-visit", "false");
                                    }

                                    // Set the appropriate animation depending on whether or not
                                    // we're reversing the very first successfull visit.
                                    if (node.getAttribute("successful-past-visit") == "false") {
                                          if (node.getAttribute("is-filled") == "true") {
                                                node.classList.add("filledVisitSuccess");
                                          }
                                          else {
                                                node.classList.add("unfilledVisitSuccess");
                                          }
                                    }
                                    else {
                                          node.classList.add("visitSuccessWithPrevSuccesses");
                                    }

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    node.addEventListener("animationend", () => {
                                          if (node.getAttribute("successful-past-visit") == "false") {
                                                if (node.getAttribute("is-filled") == "true") {
                                                      path.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);

                                                      // Set the visit count text color.
                                                      const text = textElements.item(textElements.length - 1);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                                }
                                                else {
                                                      path.setAttribute("fill", "#FFFFFF");
                                                      for (let j = 0; j < textElements.length; ++j) {
                                                            const text = textElements.item(j);
                                                            text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                                      }
                                                }
                                          }
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);
                                          node.classList.value = "nodes5";

                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          if (node.getAttribute("successful-past-visit") == "false") {
                                                if (node.getAttribute("is-filled") == "true") {
                                                      path.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);

                                                      // Set the visit count text color.
                                                      const text = textElements.item(textElements.length - 1);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                                }
                                                else {
                                                      path.setAttribute("fill", "#FFFFFF");
                                                      for (let j = 0; j < textElements.length; ++j) {
                                                            const text = textElements.item(j);
                                                            text.setAttribute("fill", nodeAndEdgeColors5["nodeDefaultFillColor"]);
                                                      }
                                                }
                                          }
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                              // When reversing a "failure", it's guaranteed that the previous state
                              // was "try" (i.e. we had to try and visit the element before being able to
                              // declare that visit a failure). We do, however, need to check whether or not
                              // the node has already been visited successfully (i.e. the fill color) in order
                              // to play the correct reverse animation.
                              else if (status == "failure") {
                                    if (node.getAttribute("successful-past-visit") == "true") {
                                          node.classList.add("visitFailureAlreadyVisited");
                                    }
                                    else {
                                          node.classList.add("visitFailureNeverVisited");
                                    }

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    node.addEventListener("animationend", () => {
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                        }
                  }

                  cnt5 -= 1;
            }

            function onClickForward5(buttonElement) {
                  if (cnt5 >= nodesAndEdgesOrdering5.length - 1) {
                        return;
                  }

                  cnt5 += 1;

                  // Get a list of the current elements that need to undergo animation.
                  const listOfElements = nodesAndEdgesOrdering5[cnt5];
                  let prevEdgeIdx = 0;

                  for (let i = 0; i < listOfElements.length; ++i) {
                        // Unpack the element attributes.
                        const elementAttrs = listOfElements[i];
                        const name = elementAttrs[0];
                        const type = elementAttrs[1];
                        const status = elementAttrs[2];

                        if (type == "edge") {
                              // Get the "edges5" element (which contains all edges), the actual current
                              // edge's html element (which is a child of the "edges5" element as well) and
                              // the corresponding path element.
                              const edges = document.getElementById("edges5");
                              const edge = document.getElementById(name);
                              const path = edge.getElementsByTagName("path").item(0);

                              // Rebuild the CSS stylesheet.
                              const edgeIdx = Array.prototype.indexOf.call(edges.children, edge) + 1;
                              prevEdgeIdx = edgeIdx;
                              dynamicStyle5.innerHTML = createCssDynamicTextContent5(0, edgeIdx, nodeAndEdgeColors5, "both", "normal");

                              // Depending on the edge status, add the corresponding name
                              // to the class and set the colors to their final values
                              // after the animation finishes.
                              if (status == "try") {
                                    edge.classList.add("visitTry");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitTryColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                              else if (status == "success") {
                                    edge.classList.add("visitSuccess");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                              else if (status == "failure") {
                                    edge.classList.add("visitFailure");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    edge.addEventListener("animationend", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitFailureColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                                    edge.addEventListener("animationcancel", () => {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitFailureColor"]);
                                          edge.classList.value = "edges5";
                                    }, { once: true });
                              }
                        }
                        else if (type == "node") {
                              // Get the "nodes5" element (which contains all nodes), the actual current
                              // nodes's html element (which is a child of the "nodes5" element as well) and
                              // the corresponding path element.
                              const nodes = document.getElementById("nodes5");
                              const node = document.getElementById(name);
                              const path = node.getElementsByTagName("path").item(0);
                              const textElements = node.getElementsByTagName("text");
                              let numCallbacks = 0;

                              // Rebuild the CSS stylesheet.
                              const nodeIdx = Array.prototype.indexOf.call(nodes.children, node) + 1;
                              dynamicStyle5.innerHTML = createCssDynamicTextContent5(nodeIdx, prevEdgeIdx, nodeAndEdgeColors5, "both", "normal");

                              // Depending on the node status, add the corresponding name
                              // to the class and set the colors to their final values
                              // after the animation finishes.
                              if (status == "try") {
                                    const g_node = document.getElementById("g");

                                    // Get the current visit count.
                                    const visitCount = elementAttrs[3];

                                    // Set the node animation based on whether or not it has already
                                    // been successfully visited.
                                    if (node.getAttribute("successful-past-visit") == "true") {
                                          node.classList.add("visitTryAlreadyVisited");
                                    }
                                    else {
                                          node.classList.add("visitTryNeverVisited");
                                    }

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-relatad classes.
                                    node.addEventListener("animationend", () => {
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);

                                          // Set the visit counter.
                                          if (textElements.length > 1 && numCallbacks == 0) {
                                                const visitCounter = textElements.item(textElements.length - 1);
                                                let vcnt5 = visitCounter.textContent;
                                                vcnt5 = vcnt5.split("/");
                                                if (vcnt5.length == 2) {
                                                      visitCounter.textContent = visitCount + "/" + vcnt5[1];
                                                }
                                                numCallbacks += 1;
                                          }

                                          node.classList.value = "nodes5";
                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitTryColor"]);

                                          // Increment the visit counter.
                                          if (textElements.length > 1 && numCallbacks == 0) {
                                                const visitCounter = textElements.item(textElements.length - 1);
                                                let vcnt5 = visitCounter.textContent;
                                                vcnt5 = vcnt5.split("/");
                                                if (vcnt5.length == 2) {
                                                      visitCounter.textContent = visitCount + "/" + vcnt5[1];
                                                }
                                                numCallbacks += 1;
                                          }

                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                              else if (status == "success") {
                                    if (node.getAttribute("successful-past-visit") == "false") {
                                          if (node.getAttribute("is-filled") == "true") {
                                                node.classList.add("filledVisitSuccess");
                                          }
                                          else {
                                                node.classList.add("unfilledVisitSuccess");
                                          }
                                    }
                                    else {
                                          node.classList.add("visitSuccessWithPrevSuccesses");
                                    }
                                    node.setAttribute("successful-past-visit", "true");

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    node.addEventListener("animationend", () => {
                                          if (node.getAttribute("is-filled") == "true") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);

                                                // Set the visit count text color.
                                                const text = textElements.item(textElements.length - 1);
                                                text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else {
                                                for (let j = 0; j < textElements.length; ++j) {
                                                      const text = textElements.item(j);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                          }
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          if (node.getAttribute("is-filled") == "true") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);

                                                // Set the visit count text color.
                                                const text = textElements.item(textElements.length - 1);
                                                text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else {
                                                for (let j = 0; j < textElements.length; ++j) {
                                                      const text = textElements.item(j);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                          }
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                              else if (status == "failure") {
                                    // Set the node's stroke color based on whether or not it has already
                                    // been successfully visited.
                                    if (node.getAttribute("successful-past-visit") == "true") {
                                          node.classList.add("visitFailureAlreadyVisited");
                                    }
                                    else {
                                          node.classList.add("visitFailureNeverVisited");
                                    }

                                    // Add event listeners for the current animation ending/cancelling early
                                    // that will correctly set the final color(s) on the element at hand, since
                                    // the colors set by the CSS animations are transient and won't stick around
                                    // if the element's class changes (which is necessary for triggering other
                                    // animations as well). Also remove all anim-related classes.
                                    node.addEventListener("animationend", () => {
                                          if (node.getAttribute("successful-past-visit") == "true") {
                                                path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else {
                                                path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                          }
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                                    node.addEventListener("animationcancel", () => {
                                          if (node.getAttribute("successful-past-visit") == "true") {
                                                path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else {
                                                path.setAttribute("stroke", nodeAndEdgeColors5["nodeDefaultStrokeColor"]);
                                          }
                                          node.classList.value = "nodes5";
                                    }, { once: true });
                              }
                        }
                  }
            }

            function onClickGoToEnd5(buttonElement) {
                  // Set counter to end.
                  cnt5 = nodesAndEdgesOrdering5.length - 1;

                  // First iterate over all nodes and set their visit counts to zero.
                  for (const child of document.getElementById("nodes5").children) {
                        const textElements = child.getElementsByTagName("text");
                        if (textElements.length <= 1) {
                              continue;
                        }
                        const visitCounter = textElements.item(textElements.length - 1);
                        let vcnt5 = visitCounter.textContent;
                        vcnt5 = vcnt5.split("/");
                        if (vcnt5.length == 2) {
                              visitCounter.textContent = "0/" + vcnt5[1];
                        }
                  }

                  for (let i = 0; i < nodesAndEdgesOrdering5.length; ++i) {
                        // Get a list of the current elements that need to be set to their end state.
                        const listOfElements = nodesAndEdgesOrdering5[i];
                        let prevEdgeIdx = 0;

                        for (let j = 0; j < listOfElements.length; ++j) {
                              // Unpack the element attributes.
                              const elementAttrs = listOfElements[j];
                              const name = elementAttrs[0];
                              const type = elementAttrs[1];
                              const status = elementAttrs[2];
                              const element = document.getElementById(name);
                              const path = element.getElementsByTagName("path").item(0);

                              // Clear the element class list.
                              element.setAttributeNS(null, "class", "");

                              // Set all elements to their final colors/states.
                              if (type == "edge") {
                                    element.classList.add("edges5");
                                    if (status == "success") {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                    }
                                    else if (status == "failure") {
                                          path.setAttribute("fill", nodeAndEdgeColors5["visitFailureColor"]);
                                    }
                              }
                              else if (type == "node") {
                                    element.classList.add("nodes5");
                                    const textElements = element.getElementsByTagName("text");

                                    if (status == "try") {
                                          // Get the visit count.
                                          const visitCount = elementAttrs[3];

                                          // Set the visit counter.
                                          if (textElements.length > 1) {
                                                const visitCounter = textElements.item(textElements.length - 1);
                                                let vcnt5 = visitCounter.textContent;
                                                vcnt5 = vcnt5.split("/");
                                                if (vcnt5.length == 2) {
                                                      visitCounter.textContent = visitCount + "/" + vcnt5[1];
                                                }
                                          }
                                    }
                                    else if (status == "success") {
                                          element.setAttribute("successful-past-visit", "true");
                                          if (element.getAttribute("is-filled") == "true") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);

                                                // Set the visit count text color.
                                                const text = textElements.item(textElements.length - 1);
                                                text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else {
                                                for (let k = 0; k < textElements.length; ++k) {
                                                      const text = textElements.item(k);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                          }
                                          path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                    }
                              }

                              // Listen for any animations that may have been playing that just
                              // ended and/or got cancelled by this action, and make sure to
                              // overwrite the final colors with the correct values after the
                              // fact.
                              element.addEventListener("animationend", () => {
                                    if (type == "edge") {
                                          element.classList.add("edges5");
                                          if (status == "success") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else if (status == "failure") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitFailureColor"]);
                                          }
                                    }
                                    else if (type == "node") {
                                          element.classList.add("nodes5");
                                          if (status == "success") {
                                                const textElements = element.getElementsByTagName("text");

                                                if (element.getAttribute("is-filled") == "true") {
                                                      path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);

                                                      // Set the visit count text color.
                                                      const text = textElements.item(textElements.length - 1);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      for (let k = 0; k < textElements.length; ++k) {
                                                            const text = textElements.item(k);
                                                            text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                      }
                                                }
                                                path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                    }
                              }, { once: true });
                              element.addEventListener("animationcancel", () => {
                                    if (type == "edge") {
                                          element.classList.add("edges5");
                                          if (status == "success") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                          else if (status == "failure") {
                                                path.setAttribute("fill", nodeAndEdgeColors5["visitFailureColor"]);
                                          }
                                    }
                                    else if (type == "node") {
                                          element.classList.add("nodes5");
                                          if (status == "success") {
                                                const textElements = element.getElementsByTagName("text");

                                                if (element.getAttribute("is-filled") == "true") {
                                                      path.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);

                                                      // Set the visit count text color.
                                                      const text = textElements.item(textElements.length - 1);
                                                      text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                }
                                                else {
                                                      for (let k = 0; k < textElements.length; ++k) {
                                                            const text = textElements.item(k);
                                                            text.setAttribute("fill", nodeAndEdgeColors5["visitSuccessColor"]);
                                                      }
                                                }
                                                path.setAttribute("stroke", nodeAndEdgeColors5["visitSuccessColor"]);
                                          }
                                    }
                              }, { once: true });
                        }
                  }
            }

            function onMouseDown5(buttonElement) {
                  let buttonPathElement = buttonElement.getElementsByTagName("path").item(0);
                  buttonPathElement.setAttribute("fill", buttonDownColor5);
            }

            function onMouseUp5(buttonElement) {
                  let buttonPathElement = buttonElement.getElementsByTagName("path").item(0);
                  buttonPathElement.setAttribute("fill", buttonHoverColor5);
            }

            function onMouseOver5(buttonElement) {
                  let buttonPathElement = buttonElement.getElementsByTagName("path").item(0);
                  buttonPathElement.setAttribute("fill", buttonHoverColor5);
            }

            function onMouseLeave5(buttonElement) {
                  let buttonPathElement = buttonElement.getElementsByTagName("path").item(0);
                  buttonPathElement.setAttribute("fill", buttonDefaultColor5);
            }

            //]]>
      </script>
</svg>