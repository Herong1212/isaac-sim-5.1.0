# Actor Simulation and Synthetic Data Generation UI

The GUI for the Isaacsim.Replicator.Agent (IRA) extension, which is a synthetic data generation tool that simulates human characters and robots in 3D environments to create high-quality training data.