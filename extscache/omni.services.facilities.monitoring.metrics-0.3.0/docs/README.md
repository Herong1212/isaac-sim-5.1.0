# Kit Services monitoring metrics facility

The metrics facility provides an easy to use interface to collect metrics as part of the services.
The facility can be injected at runtime into your service and used to collect custom metrics.

When enabled, basic metrics are enabled by default about the various service endpoints.