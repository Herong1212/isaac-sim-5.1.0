# Kit services zeroconf server transport

When enabled, provides zeroconf networking advertisment services.
Services will be advertised on the local network subnet.


## ZeroConf

To enable/disable the server set the following setting to `true` or `false`. (Default: `true`):
`exts."omni.services.transport.server.zeroconf".zeroconf.enabled`
