#usda 1.0
(
    doc ="""Generated from node description file OgnPointOnCurve.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_genproc_core_PointOnCurve" (
        docs="""Given a bundle containing curve data and a u-value (between zero and one),  get a point on the curve and the curve tangent at that point.  Note that if multiple curves are present in the bundle, only the first one will be used."""
    )
    {
        token node:type = "omni.genproc.core.PointOnCurve"
        int node:typeVersion = 1

        # 2 attributes
        custom rel inputs:curvesBundle (
            docs="""Bundle containing curves data"""
        )
        custom float inputs:uValue (
            docs="""Value from zero to one along curve at which we want to get a point"""
        )

        # 2 attributes
        custom float3 outputs:point (
            docs="""Point on input curve"""
        )
        custom float3 outputs:tangent (
            docs="""Curve tangent at point"""
        )

        # 1 attribute
        custom float state:prevUValue (
            docs="""Previous uValue value for recompute test"""
        )
    }
}
