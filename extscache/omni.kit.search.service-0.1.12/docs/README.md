# Search Service Extension [omni.kit.search.service]

This extension functions as a search engine using the `omni.kit.search_core` API.
It will register use the NGSearch Service if available, and if not it will fall back to the
default Nucleus Search Service.
