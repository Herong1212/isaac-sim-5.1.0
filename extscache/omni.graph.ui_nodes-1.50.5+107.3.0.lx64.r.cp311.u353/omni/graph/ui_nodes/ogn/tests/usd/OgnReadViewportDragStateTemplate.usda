#usda 1.0
(
    doc ="""Generated from node description file OgnReadViewportDragState.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_ui_nodes_ReadViewportDragState" (
        docs="""Read the state of the last viewport drag event from the specified viewport. Note that viewport mouse events must be enabled on the specified viewport using a SetViewportMode node."""
    )
    {
        token node:type = "omni.graph.ui_nodes.ReadViewportDragState"
        int node:typeVersion = 1

        # 3 attributes
        custom token inputs:gesture = "Left Mouse Drag" (
            docs="""The input gesture to trigger viewport drag events"""
        )
        custom bool inputs:useNormalizedCoords = false (
            docs="""When true, the components of 2D position and velocity outputs are scaled to between 0 and 1,
            where 0 is top/left and 1 is bottom/right.
            When false, components are in viewport render resolution pixels."""
        )
        custom token inputs:viewport = "Viewport" (
            docs="""Name of the viewport window to watch for drag events"""
        )

        # 5 attributes
        custom double2 outputs:currentPosition (
            docs="""The current mouse position if a drag is in progress,
            or the final mouse position of the most recent drag if a drag is not in progress"""
        )
        custom double2 outputs:initialPosition (
            docs="""The mouse position at which the most recent drag began"""
        )
        custom bool outputs:isDragInProgress (
            docs="""True if a viewport drag event is currently in progress in the specified viewport"""
        )
        custom bool outputs:isValid (
            docs="""True if a valid event state was detected and the outputs of this node are valid, and false otherwise"""
        )
        custom double2 outputs:velocity (
            docs="""A vector representing the change in position of the mouse since the previous frame
            if a drag is in progress, otherwise (0,0)"""
        )
    }
}
