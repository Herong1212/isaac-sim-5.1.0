#usda 1.0
(
    doc ="""Generated from node description file OgnPrintText.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_ui_nodes_PrintText" (
        docs="""Prints some text to the system log or to the screen"""
    )
    {
        token node:type = "omni.graph.ui_nodes.PrintText"
        int node:typeVersion = 1

        # 5 attributes
        custom uint inputs:execIn (
            docs="""Signal to the graph that this node is ready to be executed."""
        )
        custom token inputs:logLevel = "Info" (
            docs="""The logging level for the message [Info, Warning, Error]"""
        )
        custom string inputs:text = "" (
            docs="""The text to print"""
        )
        custom bool inputs:toScreen = false (
            docs="""When true, displays the text on the viewport for a few seconds, as well as the log"""
        )
        custom token inputs:viewport = "" (
            docs="""Name of the viewport if printing to screen, or empty for the default viewport"""
        )

        # 1 attribute
        custom uint outputs:execOut (
            docs="""Signal to the graph that execution can continue downstream."""
        )
    }
}
