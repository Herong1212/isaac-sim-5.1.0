#usda 1.0
(
    doc ="""Generated from node description file OgnSetViewportResolution.ogn
Contains templates for node types found in that file."""
)

def OmniGraph "TestGraph"
{
    token evaluator:type = "push"
    int2 fileFormatVersion = (1, 3)
    token flatCacheBacking = "Shared"
    token pipelineStage = "pipelineStageSimulation"

    def OmniGraphNode "Template_omni_graph_ui_nodes_SetViewportResolution" (
        docs="""Sets the resolution of the target viewport."""
    )
    {
        token node:type = "omni.graph.ui_nodes.SetViewportResolution"
        int node:typeVersion = 1

        # 3 attributes
        custom uint inputs:exec (
            docs="""Signal to the graph that this node is ready to be executed."""
        )
        custom int2 inputs:resolution = (512, 512) (
            docs="""The new resolution of the target viewport"""
        )
        custom token inputs:viewport = "Viewport" (
            docs="""Name of the viewport, or empty for the default viewport"""
        )

        # 1 attribute
        custom uint outputs:exec (
            docs="""Signal to the graph that execution can continue downstream."""
        )
    }
}
